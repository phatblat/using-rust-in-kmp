package = rust

# Path relative to cinterop working directory (repo root)
headers = rust.h
#headerFilter =

#staticLibraries = rust.a
#libraryPaths = rust/target/release

compilerOpts = -I ../../rust
#compilerOpts.osx = -Isrc/nativeInterop/cinterop/rust.def

#compilerOpts.linux = -I/usr/include -I/usr/include/x86_64-linux-gnu
#linkerOpts.osx = -L/opt/local/lib -L/usr/local/opt/curl/lib -lcurl
#linkerOpts.linux = -L/usr/lib/x86_64-linux-gnu -lcurl

linkerOpts = -L . -l rust

